<UserControl x:Class="Big3.Hitbase.CDUtilities.MultiTagEditWarningUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:sr="clr-namespace:Big3.Hitbase.SharedResources;assembly=Big3.Hitbase.SharedResources"
             mc:Ignorable="d" 
             Height="200" Width="500">
    <Grid Margin="10">
        <DockPanel>
            <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="{sr:Loc OK}" Name="buttonOK" MinWidth="75" Padding="2" />
                <Button Content="{sr:Loc Cancel}" Margin="10,0,0,0" Padding="2" Name="buttonCancel" MinWidth="80" />
            </StackPanel>
            <Grid>
                <TextBlock Text="" />
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>
