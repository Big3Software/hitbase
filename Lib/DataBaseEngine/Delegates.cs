using System;
using System.Collections.Generic;
using System.Text;

namespace Big3.Hitbase.DataBaseEngine
{
/*    public enum AddToPlaylistType
    {
        Now,
        Next,
        End
    }

    // Die Events werden benötigt, um alte C++ Komponenten starten zu können
    public delegate void OnAddTrackToWishlist(object sender, Track track);
    public delegate void OnAddTracksToPlaylist(string[] filenames, AddToPlaylistType addToPlaylistType);
    public delegate void OnUploadCD(object sender, CD cd);
    public delegate void OnPrintCDCover(object sender, CD cd);*/
}
